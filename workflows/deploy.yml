name: Build and deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install deps
        run: npm ci

      # (opcjonalnie) Lint – zostaw, jeśli masz skrypt; inaczej przeskoczy
      - name: Lint (optional)
        run: npm run lint:js || echo "no lint script, skipping"

      - name: Build 🔧
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages         # gdzie publikujemy
          folder: dist             # Vite buduje do dist (nie build)
          clean: true              # czyści stary stan gałęzi
          commit-message: 'deploy: vite build'
